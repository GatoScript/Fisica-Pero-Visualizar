import React from "react";
import { FontTitle, SectionHeader } from "./styled";

const Header = () => {
    return(
        <header>
            <article>
                <SectionHeader>
                    <FontTitle>
                        Fisica
                    </FontTitle>
                </SectionHeader>
            </article>
        </header>
    )
};

export default Header;